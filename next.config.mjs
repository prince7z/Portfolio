/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "cdn.simpleicons.org",
      "media.licdn.com",
      "img.magnific.com",
      "api.iconify.design",
    ],
  },
};

export default nextConfig;
